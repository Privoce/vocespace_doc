import { Tab, Tabs } from "@theme";

# Справка

## Проверка архитектуры Linux

VoceSpace имеет два типа образов:
- amd: `privoce/vocespace:latest`
- arm: `privoce/vocespace:latest_arm`

Поэтому вам нужно знать архитектуру вашего сервера для выбора

### 1. Использование команды `uname`

Команда `uname` может отображать информацию о ядре системы, включая аппаратную архитектуру.

Выполните следующую команду:
```bash
uname -m
```

- Если вывод `x86_64`, то система основана на архитектуре AMD64 или Intel64 (обычно называется x86_64).
- Если вывод `aarch64` или `arm64`, то система основана на архитектуре ARM64.
- Если вывод `armv7l` или подобное, то система основана на архитектуре ARMv7.

### 2. Использование команды `arch`
Команда `arch` также может отображать тип архитектуры системы.

Выполните следующую команду:
```bash
arch
```

- Если вывод `x86_64`, то система основана на архитектуре AMD64 или Intel64.
- Если вывод `aarch64` или `arm64`, то система основана на архитектуре ARM64.
- Если вывод `armv7l` или подобное, то система основана на архитектуре ARMv7.

## Как получить key и secret

```bash
docker pull livekit/generate
docker run --rm -it -v $PWD:/output livekit/generate
```

С помощью `livekit/generate` вы получите файл `livekit.yaml`, который является файлом конфигурации вашего livekit-server, в нем будут сгенерированы key и secret

## Как повысить качество видео/демонстрации экрана

| Разрешение          | Пиксели   | FPS   | Кодек  | Рекомендуемый битрейт | Высокое качество | Максимальное качество |
| ------------------- | --------- | ----- | ------ | -------------------- | ---------------- | --------------------- |
| **540p (qHD)**      | 960×540   | 30fps | H.264  | 800 Kbps             | 1 Mbps           | 1.5 Mbps              |
|                     |           |       | VP9    | 600 Kbps             | 800 Kbps         | 1.2 Mbps              |
|                     |           |       | AV1    | 400 Kbps             | 700 Kbps         | 1 Mbps                |
| **720p (HD)**       | 1280×720  | 30fps | H.264  | 1.5 Mbps             | 2 Mbps           | 2.5 Mbps              |
|                     |           | 60fps | H.264  | 2.5 Mbps             | 3.5 Mbps         | 4 Mbps                |
|                     |           |       | VP9    | 1.2 Mbps             | 1.8 Mbps         | 2.2 Mbps              |
|                     |           |       | AV1    | 1 Mbps               | 1.5 Mbps         | 2 Mbps                |
| **1080p (Full HD)** | 1920×1080 | 30fps | H.264  | 3 Mbps               | 4.5 Mbps         | 6 Mbps                |
|                     |           | 60fps | H.264  | 4.5 Mbps             | 6 Mbps           | 8 Mbps                |
|                     |           |       | VP9    | 2.5 Mbps             | 4 Mbps           | 5 Mbps                |
|                     |           |       | AV1    | 1.8 Mbps             | 3 Mbps           | 4 Mbps                |
| **1440p (2K/QHD)**  | 2560×1440 | 30fps | H.264  | 6 Mbps               | 8 Mbps           | 10 Mbps               |
|                     |           | 60fps | H.264  | 8 Mbps               | 10 Mbps          | 14 Mbps               |
|                     |           |       | VP9    | 4.5 Mbps             | 7 Mbps           | 9 Mbps                |
|                     |           |       | AV1    | 3.5 Mbps             | 5 Mbps           | 7 Mbps                |
| **2160p (4K/UHD)**  | 3840×2160 | 30fps | H.264  | 10 Mbps              | 14 Mbps          | 20 Mbps               |
|                     |           | 60fps | H.264  | 15 Mbps              | 20 Mbps          | 25 Mbps               |
|                     |           |       | VP9    | 8 Mbps               | 12 Mbps          | 18 Mbps               |
|                     |           |       | AV1    | 6 Mbps               | 10 Mbps          | 14 Mbps               |

### Пояснения

* **Рекомендуемый битрейт**: подходит для ежедневных трансляций, конференций, потоковой передачи с низкой задержкой.
* **Высокое качество**: подходит для загрузки на YouTube или записи, баланс между качеством и размером файла.
* **Максимальное качество**: для высококачественного сохранения или профессиональных сценариев, таких как сбор материалов, исходники для видеомонтажа.
* **Описание кодеков**:

  * **H.264**: лучшая совместимость, широкая аппаратная поддержка.
  * **VP9**: разработан Google, широко используется YouTube, эффективность немного выше, чем у H.264.
  * **AV1**: кодек следующего поколения с высоким коэффициентом сжатия, меньший размер файла, но более длительное время кодирования.


### Соответствующие параметры запуска

- Разрешение: `NEXT_PUBLIC_RESOLUTION`
- Битрейт: `NEXT_PUBLIC_MAXBITRATE`
- Частота кадров: `NEXT_PUBLIC_MAXFRAMERATE`
- Приоритет: `NEXT_PUBLIC_PRIORITY`
  - high
  - medium
  - low
  - very-low

### Пример

Когда вам нужно использовать 720p + 60fps + стратегию высокой точности

```bash 
bash ./local_deploy.sh \
-e NEXT_PUBLIC_RESOLUTION=720p \
-e NEXT_PUBLIC_MAXFRAMERATE=60 \
-e NEXT_PUBLIC_PRIORITY=high
```

## Скрипт локального развертывания

<Tabs>
  <Tab label="Linux/macOS">
    ```shell
    #!/bin/bash

    # Скрипт автоматического развертывания VoceSpace
    # Для систем macOS и Linux

    set -e # Выход при ошибке

    # Переменные окружения по умолчанию
    CUSTOM_ENV_VARS=()

    # Показать справку по использованию
    show_help() {
    echo "================================================"
    echo "Скрипт автоматического развертывания VoceSpace"
    echo "================================================"
    echo ""
    echo "Использование: $0 [опции]"
    echo ""
    echo "Опции:"
    echo " -e, --env KEY=VALUE Установить пользовательскую переменную окружения"
    echo " -h, --help Показать эту справку"
    echo ""
    echo "Примеры:"
    echo " $0 -e NEXT_PUBLIC_RESOLUTION=4k
    echo " $0 -e NEXT_PUBLIC_RESOLUTION=1080p"
    echo " $0 -e NEXT_PUBLIC_RESOLUTION=4k -e CUSTOM_VAR=value"
    echo " $0 -e NEXT_PUBLIC_MAXBITRATE=8000 -e NEXT_PUBLIC_RESOLUTION=720p"
    echo ""
    echo "Общие переменные окружения:
    echo " - NEXT_PUBLIC_RESOLUTION: Установить разрешение (4k, 2k, 1080p, 720p, 480p)"
    echo " - NEXT_PUBLIC_MAXBITRATE: Установить максимальный битрейт"
    echo " - NEXT_PUBLIC_MAXFRAMERATE: Установить максимальную частоту кадров"
    echo " - NEXT_PUBLIC_PRIORITY: Установить приоритет"
    echo ""
    }

    # Разбор аргументов командной строки
    parse_args() {
        while [[ $# -gt 0 ]]; do
            case $1 in
                -e|--env)
                    CUSTOM_ENV_VARS+=("$2")
                    shift 2
                    ;;
                -h|--help)
                    show_help
                    exit 0
                    ;;
                *)
                    echo "❌ Неизвестный параметр: $1"
                    echo "Используйте $0 --help для просмотра справки"
                    exit 1
                    ;;
            esac
        done
    }

    echo "=========================================="
    echo "Скрипт автоматического развертывания VoceSpace"
    echo "=========================================="

    # Определение операционной системы
    OS=$(uname -s)
    ARCH=$(uname -m)

    echo "Операционная система: $OS"
    echo "Архитектура: $ARCH"

    # Функция: Проверка существования команды
    check_command() {
        if ! command -v "$1" &> /dev/null; then
            echo "❌ $1 не установлен"
            return 1
        else
            echo "✅ $1 установлен"
            return 0
        fi
    }

    # Функция: Проверка установки Docker
    check_docker() {
        if check_command docker; then
            echo "Версия Docker: $(docker --version)"
            return 0
        else
            echo "❌ Docker не установлен, установите Docker сначала"
            echo "Посетите следующие ссылки для установки Docker:"
            case $OS in
                "Darwin")
                    echo "  macOS: https://docs.docker.com/desktop/install/mac-install/"
                    ;;
                "Linux")
                    echo "  Linux: https://docs.docker.com/engine/install/"
                    ;;
                *)
                    echo "  другие: https://docs.docker.com/get-docker/"
                    ;;
            esac
            return 1
        fi
    }

    # Установка LiveKit Server
    install_livekit() {
        echo "=========================================="
        echo "Установка LiveKit Server"
        echo "=========================================="
        
        if check_command livekit-server; then
            echo "LiveKit Server уже установлен"
            return 0
        fi
        
        case $OS in
            "Darwin")
                echo "Установка LiveKit Server на macOS..."
                if check_command brew; then
                    brew update && brew install livekit
                else
                    echo "❌ Homebrew не установлен, установите Homebrew или установите LiveKit Server вручную"
                    echo "Команда установки Homebrew: /bin/bash -c \"\$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)\""
                    return 1
                fi
                ;;
            "Linux")
                echo "Установка LiveKit Server на Linux..."
                curl -sSL https://get.livekit.io | bash
                ;;
            *)
                echo "❌ Неподдерживаемая операционная система: $OS"
                echo "Установите LiveKit Server вручную: https://github.com/livekit/livekit/releases"
                return 1
                ;;
        esac
    }

    # Функция: Получить Docker образ
    get_docker_image() {
        case $ARCH in
            "x86_64"|"amd64")
                echo "privoce/vocespace:latest"
                ;;
            "arm64"|"aarch64")
                echo "privoce/vocespace:latest_arm"
                ;;
            *)
                echo "❌ Неподдерживаемая архитектура: $ARCH"
                echo "Поддерживаемые архитектуры: x86_64, amd64, arm64, aarch64"
                exit 1
                ;;
        esac
    }

    # Функция: Загрузить образ VoceSpace
    pull_vocespace_image() {
        echo "=========================================="
        echo "Загрузка образа VoceSpace Docker"
        echo "=========================================="
        
        IMAGE=$(get_docker_image)
        echo "Загрузка образа: $IMAGE"

        docker pull "$IMAGE"
        echo "✅ Образ успешно загружен"
    }

    # Функция: Запустить контейнер VoceSpace
    start_vocespace_container() {
        echo "=========================================="
        echo "Запуск контейнера VoceSpace"
        echo "=========================================="
        
        IMAGE=$(get_docker_image)
        CONTAINER_NAME="vocespace"

        # Построить аргументы переменных окружения
        ENV_ARGS=()

        # Добавить пользовательские переменные окружения
        for env_var in "${CUSTOM_ENV_VARS[@]}"; do
            ENV_ARGS+=("-e" "$env_var")
            echo "Установка переменной окружения: $env_var"
        done

        # Проверить, существует ли контейнер
        if docker ps -a --format "table {{.Names}}" | grep -q "^$CONTAINER_NAME$"; then
            echo "Обнаружен существующий контейнер $CONTAINER_NAME"
            read -p "Остановить и удалить существующий контейнер? (y/n): " -n 1 -r
            echo
            if [[ $REPLY =~ ^[Yy]$ ]]; then
                echo "Остановка и удаление существующего контейнера..."
                docker stop "$CONTAINER_NAME" 2>/dev/null || true
                docker rm "$CONTAINER_NAME" 2>/dev/null || true
            else
                echo "Операция отменена"
                return 1
            fi
        fi

        echo "Запуск нового контейнера..."
        if [[ ${#ENV_ARGS[@]} -gt 0 ]]; then
            echo "Использование переменных окружения: ${ENV_ARGS[*]}"
            docker run -d \
                -p 3000:3000 \
                --name "$CONTAINER_NAME" \
                "${ENV_ARGS[@]}" \
                "$IMAGE"
        else
            docker run -d \
                -p 3000:3000 \
                --name "$CONTAINER_NAME" \
                "$IMAGE"
        fi

        echo "✅ Контейнер VoceSpace запущен"
        echo "Имя контейнера: $CONTAINER_NAME"
        echo "URL доступа: http://localhost:3000"
    }

    # Функция: Запустить LiveKit Server
    start_livekit_server() {
        echo "=========================================="
        echo "Запуск LiveKit Server"
        echo "=========================================="

        echo "Проверка, запущен ли LiveKit Server..."
        if pgrep -f "livekit-server" > /dev/null; then
            echo "⚠️  LiveKit Server уже запущен"
            read -p "Перезапустить LiveKit Server? (y/n): " -n 1 -r
            echo
            if [[ $REPLY =~ ^[Yy]$ ]]; then
                echo "Остановка существующего LiveKit Server..."
                pkill -f "livekit-server" || true
                sleep 2
            else
                echo "Оставить существующий LiveKit Server запущенным"
                return 0
            fi
        fi

        echo "Запуск LiveKit Server..."
        echo "Примечание: LiveKit Server будет работать в фоновом режиме"
        echo "Для остановки используйте: pkill -f livekit-server"

        nohup livekit-server --dev --bind 0.0.0.0 > livekit-server.log 2>&1 &
        
        sleep 3
        
        if pgrep -f "livekit-server" > /dev/null; then
            echo "✅ LiveKit Server успешно запущен"
            echo "Файл логов: $(pwd)/livekit-server.log"
        else
            echo "❌ Не удалось запустить LiveKit Server"
            echo "Проверьте файл логов: $(pwd)/livekit-server.log"
            return 1
        fi
    }

    # Функция: Показать статус
    show_status() {
        echo "=========================================="
        echo "Текущий статус"
        echo "=========================================="
        
        echo "Статус контейнера Docker:"
        docker ps --filter "name=vocespace" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
        
        echo ""
        echo "Статус LiveKit Server:"
        if pgrep -f "livekit-server" > /dev/null; then
            echo "✅ LiveKit Server запущен"
        else
            echo "❌ LiveKit Server не запущен"
        fi
        
        echo ""
        echo "=========================================="
        echo "Информация о доступе"
        echo "=========================================="
        echo "VoceSpace App: http://localhost:3000"
        echo "LiveKit Server: ws://localhost:7880"
        echo ""
        echo "Для просмотра логов контейнера: docker logs vocespace"
        echo "Для просмотра логов LiveKit: tail -f livekit-server.log"
    }

    # Главная функция
    main() {
        # Разбор аргументов командной строки
        parse_args "$@"

        echo "Начало процесса развертывания..."

        # Показать информацию о конфигурации
        if [[ ${#CUSTOM_ENV_VARS[@]} -gt 0 ]]; then
            echo "=========================================="
            echo "Информация о конфигурации"
            echo "=========================================="
            for env_var in "${CUSTOM_ENV_VARS[@]}"; do
                echo "Переменная окружения: $env_var"
            done
            echo ""
        fi

        # Проверка Docker
        if ! check_docker; then
            exit 1
        fi

        # Установка LiveKit Server
        if ! install_livekit; then
            echo "❌ Не удалось установить LiveKit Server"
            exit 1
        fi

        # Загрузка образа Docker
        if ! pull_vocespace_image; then
            echo "❌ Не удалось загрузить образ Docker"
            exit 1
        fi

        # Запуск контейнера
        if ! start_vocespace_container; then
            echo "❌ Не удалось запустить контейнер"
            exit 1
        fi

        # Запуск LiveKit Server
        if ! start_livekit_server; then
            echo "❌ Не удалось запустить LiveKit Server"
            exit 1
        fi
        
        echo ""
        echo "Ожидание запуска служб..."
        sleep 5

        # Показать статус
        show_status
        
        echo ""
        echo "==========================================="
        echo "🎉 Развертывание завершено!"
        echo "==========================================="
        echo "Откройте http://localhost:3000 в браузере"
        echo "Если возникли проблемы, проверьте соответствующие файлы логов"
    }

    # Запустить главную функцию
    main "$@"

    ```
  </Tab>
  <Tab label="Windows">
    ```shell
    @echo off
    setlocal EnableDelayedExpansion

    REM Скрипт автоматического развертывания VoceSpace (Windows)
    REM Для систем Windows

    REM Переменные окружения по умолчанию
    set ENV_VARS=

    REM Показать справку по использованию
    :show_help
    echo ==========================================
    echo Скрипт автоматического развертывания VoceSpace (Windows)
    echo ==========================================
    echo.
    echo Использование: %~nx0 [опции]
    echo.
    echo Опции:
    echo   -e, --env KEY=VALUE            Установить пользовательскую переменную окружения
    echo   -h, --help                     Показать эту справку
    echo.
    echo Примеры:
    echo   %~nx0 -e NEXT_PUBLIC_RESOLUTION=4k
    echo   %~nx0 -e NEXT_PUBLIC_RESOLUTION=1080p
    echo   %~nx0 -e NEXT_PUBLIC_RESOLUTION=4k -e CUSTOM_VAR=value
    echo   %~nx0 -e NEXT_PUBLIC_MAXBITRATE=8000 -e NEXT_PUBLIC_RESOLUTION=720p
    echo.
    echo Общие переменные окружения:
    echo   - NEXT_PUBLIC_RESOLUTION: Установить разрешение (4k, 2k, 1080p, 720p, 480p)
    echo   - NEXT_PUBLIC_MAXBITRATE: Установить максимальный битрейт
    echo   - NEXT_PUBLIC_MAXFRAMERATE: Установить максимальную частоту кадров
    echo   - NEXT_PUBLIC_PRIORITY: Установить приоритет
    echo.
    goto :eof

    REM Разбор аргументов командной строки
    :parse_args
    if "%~1"=="" goto start_main
    if "%~1"=="-h" goto show_help
    if "%~1"=="--help" goto show_help
    if "%~1"=="-e" (
        if "!ENV_VARS!"=="" (
            set ENV_VARS=-e %~2
        ) else (
            set ENV_VARS=!ENV_VARS! -e %~2
        )
        shift
        shift
        goto parse_args
    )
    if "%~1"=="--env" (
        if "!ENV_VARS!"=="" (
            set ENV_VARS=-e %~2
        ) else (
            set ENV_VARS=!ENV_VARS! -e %~2
        )
        shift
        shift
        goto parse_args
    )
    echo ❌ Неизвестный параметр: %~1
    echo Используйте %~nx0 --help для просмотра справки
    exit /b 1

    :start_main
    REM Вызов разбора параметров
    call :parse_args %*

    echo ==========================================
    echo Скрипт автоматического развертывания VoceSpace (Windows)
    echo ==========================================

    REM Показать используемые переменные окружения
    if not "!ENV_VARS!"=="" (
        echo Будут использованы следующие переменные окружения: !ENV_VARS!
    )

    REM Определение архитектуры системы
    set ARCH=%PROCESSOR_ARCHITECTURE%
    echo Обнаружена архитектура: %ARCH%

    REM Проверка установки Docker
    echo Проверка установки Docker...
    docker --version >nul 2>&1
    if %errorlevel% neq 0 (
        echo ❌ Docker не установлен
        echo Установите Docker Desktop для Windows:
        echo https://docs.docker.com/desktop/install/windows-install/
        pause
        exit /b 1
    ) else (
        echo ✅ Docker установлен
        docker --version
    )

    REM Определение образа Docker
    if "%ARCH%"=="AMD64" (
        set IMAGE=privoce/vocespace:latest
        echo Использование образа AMD64: %IMAGE%
    ) else if "%ARCH%"=="ARM64" (
        set IMAGE=privoce/vocespace:latest_arm
        echo Использование образа ARM64: %IMAGE%
    ) else (
        echo ❌ Неподдерживаемая архитектура: %ARCH%
        echo Поддерживаемые архитектуры: AMD64, ARM64
        pause
        exit /b 1
    )

    REM Загрузка образа VoceSpace
    echo ==========================================
    echo Загрузка образа VoceSpace Docker
    echo ==========================================
    echo Загрузка образа: %IMAGE%

    docker pull %IMAGE%
    if %errorlevel% neq 0 (
        echo ❌ Не удалось загрузить образ
        pause
        exit /b 1
    )
    echo ✅ Образ успешно загружен

    REM Запуск контейнера VoceSpace
    echo ==========================================
    echo Запуск контейнера VoceSpace
    echo ==========================================

    set CONTAINER_NAME=vocespace

    REM Проверка существования контейнера
    docker ps -a --filter "name=%CONTAINER_NAME%" --format "{{.Names}}" | findstr /x "%CONTAINER_NAME%" >nul 2>&1
    if %errorlevel% equ 0 (
        echo Обнаружен существующий контейнер %CONTAINER_NAME%
        set /p CHOICE="Остановить и удалить существующий контейнер? (y/n): "
        if /i "!CHOICE!"=="y" (
            echo Остановка и удаление существующего контейнера...
            docker stop %CONTAINER_NAME% >nul 2>&1
            docker rm %CONTAINER_NAME% >nul 2>&1
        ) else (
            echo Операция отменена
            pause
            exit /b 1
        )
    )

    echo Запуск нового контейнера...
    if "!ENV_VARS!"=="" (
        docker run -d -p 3000:3000 --name %CONTAINER_NAME% %IMAGE%
    ) else (
        docker run -d -p 3000:3000 !ENV_VARS! --name %CONTAINER_NAME% %IMAGE%
    )
    if %errorlevel% neq 0 (
        echo ❌ Не удалось запустить контейнер
        pause
        exit /b 1
    )

    echo ✅ Контейнер VoceSpace успешно запущен
    echo Имя контейнера: %CONTAINER_NAME%
    echo Адрес доступа: http://localhost:3000

    REM Инструкции по LiveKit Server
    echo ==========================================
    echo Инструкции по установке LiveKit Server
    echo ==========================================
    echo Для систем Windows загрузите и установите LiveKit Server вручную:
    echo 1. Посетите: https://github.com/livekit/livekit/releases
    echo 2. Загрузите версию для вашей системы
    echo 3. Распакуйте в подходящую директорию
    echo 4. Запустите в командной строке: livekit-server --dev --bind 0.0.0.0
    echo.

    REM Проверка доступности LiveKit Server
    livekit-server --help >nul 2>&1
    if %errorlevel% equ 0 (
        echo ✅ LiveKit Server установлен
        echo Запуск LiveKit Server...
        start /b livekit-server --dev --bind 0.0.0.0
        timeout /t 3 /nobreak >nul
        echo ✅ LiveKit Server запущен
    ) else (
        echo ⚠️  LiveKit Server не найден, следуйте инструкциям выше для установки
    )

    REM Показать статус
    echo ==========================================
    echo Проверка статуса развертывания
    echo ==========================================

    echo Статус контейнера Docker:
    docker ps --filter "name=%CONTAINER_NAME%" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

    echo.
    echo ==========================================
    echo Информация о доступе
    echo ==========================================
    echo Приложение VoceSpace: http://localhost:3000
    echo LiveKit Server: ws://localhost:7880
    echo.
    echo Для просмотра логов контейнера: docker logs %CONTAINER_NAME%
    echo.

    echo ==========================================
    echo 🎉 Развертывание завершено!
    echo ==========================================
    echo Откройте http://localhost:3000 в браузере
    echo При возникновении проблем проверьте соответствующие логи

    pause

    ```
  </Tab>
</Tabs>


## Тайм-аут сетевого подключения Docker

### 👍 Использование зеркал для Китая

#### AMD64

```bash
docker pull crpi-0u8xaje6k9g42uky.cn-hangzhou.personal.cr.aliyuncs.com/vocespace/vocespace:latest
```

#### ARM64

```bash
docker pull crpi-0u8xaje6k9g42uky.cn-hangzhou.personal.cr.aliyuncs.com/vocespace/vocespace:latest_arm
```

### Настройка Docker

Пользователи в Китае могут столкнуться с проблемами тайм-аута при доступе к Docker Hub. Следуйте приведенным ниже инструкциям для настройки.

#### Создание daemon.json

```bash
sudo mkdir -p /etc/docker
sudo touch /etc/docker/daemon.json
```

#### Добавление зеркал репозиториев

- Tencent: `https://mirror.ccs.tencentyun.com`
- USTC: `https://docker.mirrors.ustc.edu.cn`
- Alibaba: см. Консоль -> Поиск -> Сервис контейнерных образов ACR -> Инструменты образов -> Ускорение образов

```bash
sudo tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": ["https://mirror.ccs.tencentyun.com", "https://docker.mirrors.ustc.edu.cn"]
}
EOF
```

#### Перезапуск Docker

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### Смена сервера

В Китае, даже если сервер имеет публичный IP, могут возникнуть проблемы с доступом к внешним сервисам, таким как Docker Hub и GitHub. Это в основном связано со сложностью сетевой среды и связанными с ней ограничениями. Возможные причины и решения:
Общие причины

- Проблемы с разрешением DNS: DNS-серверы по умолчанию могут не корректно разрешать доменные имена Docker Hub или GitHub
- Сетевые ограничения: некоторые сетевые среды (например, сети операторов, корпоративные сети) могут ограничивать или блокировать доступ к определенным внешним сервисам
- Брандмауэр или политики безопасности: брандмауэр сервера или правила группы безопасности могут блокировать доступ к определенным сервисам
- Влияние GFW: в Китае некоторые внешние сервисы могут быть ограничены GFW

> [!TIP]
>
> Поэтому мы рекомендуем выбирать серверы в следующих регионах:
> 1. Гонконг (Китай)
> 2. Япония
> 3. Корея
> 4. Сингапур
> 5. Страны Европы и Америки

## Часто используемые команды Docker

### Просмотр образов

```bash
docker image ls
# или используйте
docker images
```

### Просмотр всех контейнеров

```bash
docker ps -a
```

### Просмотр запущенных контейнеров

```bash
docker ps
```

### Остановка контейнера

```bash
docker stop container_name_or_id
```

### Принудительное завершение контейнера

```bash
docker kill container_name_or_id
```

### Удаление контейнера

```bash
docker rm container_name_or_id

# Удаление остановленных контейнеров
docker container prune
# Удаление всех, включая запущенные
docker rm -f $(docker ps -aq)
```

### Удаление образа

```bash
docker rmi image_name_or_id

# Удаление всех образов
docker rmi -f $(docker images -q)
```